<commands>
   <command>
      <name>add-form</name>
      <title>Add a form to the module</title>
      <usage>add-form MODULE FORM-NAME</usage>
      <help><![CDATA[
  This command adds a new form to the given module.
  The form is intended to be included from another XHTML file by using
  a <ui:include> directive (it does not contain the <f:view> component).
  The associated response is a JSON that allows to perform update operations.

    web/<module>/forms/<form-name>-form.xhtml
    web/<module>/forms/<form-name>-response.xhtml
]]>   </help>
      <param name='moduleName'/>
      <param name='formName'/>
      <basedir>web</basedir>
      <template>add-form/form.xhtml</template>
      <template>add-form/form-response.xhtml</template>
   </command>
</commands>
